import React from "react";
import "input.scss";
const Input = ({}) => {
  return (
    <input
      id="fullname"
      type="text"
      placeholder="Enter Your Full Name"
      className="input"
    />
  );
};

export default Input;
